<div class="event-box">

<%= render 'form' %>

<p>
	<%= link_to 'Delete', event_path(@event), method: :delete, class: "btn btn-danger btn-margin", "data-confirm" => "Are you sure?" %>
	<%= link_to 'Back', @event, class: "btn btn-link btn-margin" %>
</p>

</div>
