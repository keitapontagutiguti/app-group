<div class="event-box">

<%= render 'form' %>

<p><%= link_to 'Delete', event_path(@event), method: :delete, class: "btn btn-danger" %></p>

<p><%= link_to 'Back', @event, class: "btn btn-link" %></p>

</div>
